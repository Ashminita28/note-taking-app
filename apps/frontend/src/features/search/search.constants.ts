/** Debounce period — canonical source: FRS UX-SRCH-01 / SDS §24.5. */
export const SEARCH_DEBOUNCE_MS = 300;

/** Shimmer skeleton count while a search request is in flight — UX §11. */
export const SEARCH_RESULTS_SKELETON_COUNT = 3;
